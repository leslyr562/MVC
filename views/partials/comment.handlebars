<div class="card">
  {{#each this}}
  <section class="card-header">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="card-body">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>